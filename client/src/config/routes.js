const routes = {
    login: '/login',
    register: '/register',
    finalregister: '/finalregister/:status',
    resetpassword: '/resetpassword/:token',
    diarypost: '/',
    setting: '/setting',
    
};

export default routes;